<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	x:Class="CustomPicker.MainPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:components="clr-namespace:CustomPicker.Components">

	<ScrollView>
		<VerticalStackLayout Padding="30,0" Spacing="25">
			<Image
				Aspect="AspectFit"
				HeightRequest="185"
				SemanticProperties.Description="dot net bot in a hovercraft number nine"
				Source="dotnet_bot.png" />

			<Label
				SemanticProperties.HeadingLevel="Level1"
				Style="{StaticResource Headline}"
				Text="Hello, World!" />

			<Label
				SemanticProperties.Description="Welcome to dot net Multi platform App U I"
				SemanticProperties.HeadingLevel="Level2"
				Style="{StaticResource SubHeadline}"
				Text="Welcome to &#10;.NET Multi-platform App UI" />

			<Border
				Padding="10"
				Stroke="Gray"
				StrokeShape="RoundRectangle 10,10,10,10">
				<Border.GestureRecognizers>
					<TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />
				</Border.GestureRecognizers>
				<Label x:Name="lblDropDown" />
			</Border>

			<components:LanguageSelection ItemSource="{Binding Cultures}" SelectedItem="{Binding SelectedCulture}">
				<components:LanguageSelection.ItemTemplate>
					<DataTemplate>
						<StackLayout Padding="10">
							<Label
								FontSize="16"
								Text="{Binding DisplayName}"
								TextColor="Black" />
						</StackLayout>
					</DataTemplate>
				</components:LanguageSelection.ItemTemplate>
			</components:LanguageSelection>

			<components:LanguageDropDown ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}">
				<components:LanguageDropDown.ItemTemplate>
					<DataTemplate>
						<Grid Margin="5,5,5,5">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<Label
								FontSize="16"
								Text="{Binding Name}"
								TextColor="Black" />
							<Label
								Grid.Column="1"
								FontSize="16"
								HorizontalOptions="EndAndExpand"
								Text="{Binding Code}"
								TextColor="Gray" />
						</Grid>
					</DataTemplate>
				</components:LanguageDropDown.ItemTemplate>
			</components:LanguageDropDown>
			<Label Text="{Binding SelectedLanguage.Code}" />

			<components:EntryBox Text="{Binding SelectedText}" />

			<components:EntryBox Text="{Binding SelectedText1}" ValidationRule="{Binding TextValidation}" />
			<Label Text="{Binding SelectedText1}" />

			<components:EntryBox Text="{Binding SelectedText2}" MustNotEmpty="True" />

			<Button
				x:Name="CounterBtn"
				Clicked="OnCounterClicked"
				HorizontalOptions="Fill"
				SemanticProperties.Hint="Counts the number of times you click"
				Text="Click me" />
		</VerticalStackLayout>
	</ScrollView>

</ContentPage>
